run 
$ sudo apt install docker.io
$ sudo ./do-docker-build.sh 
to do an ffmpeg build in a docker instance, then it copies the ffmpeg.exe's to your local dir, and destroys the docker instance...
you can customize it as desired
